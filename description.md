# Постановка задачи

Необходимо решить задачу классификации для операций, которые по различным причинам, связанным с безопасностью, были не подтверждены клиентами банка. То есть, участникам предстоит разработать алгоритм, реализующий ядро системы антифрода для предотвращения мошеннических операций.

## Структура данных

Для обучения моделей предоставляются операции 100,000 клиентов за 1.5 года, организованные по времени на 4 отдельных периода:

- **Pre-train** (2023-10-01 — 2024-09-30)  
  История операций клиентов, которую можно использовать для предобучения и извлечения признаков. Не имеет разметки по построению.

- **Train** (2024-10-01 — 2025-05-31)  
  История операций клиентов, в которой у них появляются нежелательные операции. Имеет разметку:
  - 🔴 **Неподтвержденные операции** (целевой класс, "красный свет")
  - 🟡 **Подозрительные операции, подтвержденные клиентами** ("желтый свет", не является целевым классом)
  - 🟢 **Все остальные операции** без обратной связи клиента считаются подтвержденными ("зеленый свет")

- **Pre-test** (2025-06-01 — 2025-08-09)  
  История операций клиентов, которую можно использовать для классификации самих тестовых меток. Не имеет разметки, так как является частью тестовых данных.

- **Test** (2025-06-01 — 2025-08-09)  
  Заключительный день истории операций клиентов, каждую из которых требуется классифицировать и отправить в рамках соревнования. У каждого клиента этот заключительный день выбран случайно.

## Особенности задачи

У задачи имеется ряд особенностей для исследователей и практиков анализа данных:

- **Целевой класс очень редкий**: в тренировочных данных всего 51 тысяча неподтвержденных операций (🔴) и 36 тысяч подозрительных подтвержденных (🟡)
- **Временная зависимость**: алгоритм должен работать в приближенной к реальной постановке, с учетом времени. Как если бы классификатор работал здесь и сейчас, а не размечал уже ранее случившиеся инциденты пост-фактум
- **Большой объем данных**: суммарный объем данных в задаче превышает 200 миллионов операций

## Формат решений

Это табличное соревнование с разметкой предоставленного вам `.csv` файла. Вам необходимо создать алгоритм, способный по предоставленным в рамках соревнования данным создать новый табличный `.csv` файл с двумя столбцами:

```csv
event_id, predict
125854726334416, -0.338988
125949211749418, -4.100378
...
124738035029214, 0.004335
```

Описание столбцов:
- `event_id` – идентификатор операции
- `predict` – предсказание вашего алгоритма

Предсказания необходимо построить для всех **633,683** операций тестовых данных.

Пример `sample_submit.csv` доступен на странице "Данные".

## Проверка решений

Решения проверяются автоматически путем сопоставления с известной истинной разметкой статусов операций по классу неподтвержденных операций (🔴) в тестовый период данных. Истинная разметка тестовых данных доступна только организаторам.

### Метрика

**Метрика соревнования**: PR-AUC (Area Under Precision-Recall Curve; площадь под кривой «полнота-точность») по операциям.

Для расчета метрики используется её sklearn имплементация:

```python
from sklearn.metrics import average_precision_score
```

> ⚠️ **Обратите внимание**: использование функции `auc` для самой Precision-Recall кривой средствами sklearn приведет к завышению показателей метрики ввиду особенностей процедуры подсчета (интерполяции точек кривой).

### Разделение данных

Тестовые данные представляют собой 70 дней операций, сгруппированные в 10 недель.  
Отсчет идет с 2025-06-01 по 2025-08-09, первая неделя идет с 2025-06-01 по 2025-06-07, и т.д.

Соотношение public/private в соревновании составляет **30/70** и разделено по времени (по неделям):

- **Public лидерборд**: недели 1, 3 и 5
- **Private лидерборд**: остальные 7 недель

Победители соревнования определяются по результатам на **private лидерборде**.  
Для private лидерборда можно выбрать до **2-ух финальных решений**.

## Данные

Для решения задачи 1 "Страж" предлагается несколько наборов данных и материалов. Данные разделены на 4 временных периода:

- **Pre-train** (2023-10-01 — 2024-09-30) — только данные самих операций
- **Train** (2024-10-01 — 2025-05-31) — данные операций и разметка целевой переменной к ним
- **Pre-test** (2025-06-01 — 2025-08-09) — начало тестовых данных с историей операций, но без разметки к ним
- **Test** (2025-06-01 — 2025-08-09) — финальный день тестовых данных операций клиентов, по которому требуется построить разметку и отправить ее в качестве решения

Для удобства участников, pre-train и train данные разделены на 3 части. Каждый файл содержит свою группу клиентов на основе их `customer_id`. Сами записи отсортированы по клиентам `customer_id` и по времени произведения операций `event_dttm`.

## Основные поля данных

- `customer_id` – ID клиента банка
- `event_id` — ID операции
- `event_dttm` — дата и время операции
- `target` – целевая переменная, класс операции:
  - `1` соответствует неподтвержденной операции (🔴)
  - `0` соответствует напрямую подтвержденной клиентом операции (🟡)
  - Все остальные операции в данных считаются операциями без обратной связи (🟢)
